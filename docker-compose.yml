services:
  backend:
    build: ./backend
    container_name: currency-widget-backend
    expose:
      - "3000"
    environment:
      NODE_ENV: production
      PORT: 3000

  frontend:
    build: ./frontend
    container_name: currency-widget-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
